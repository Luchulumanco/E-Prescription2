@model IEnumerable<E_Prescription2.Models.DosageForm>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DosageFormName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DosageFormName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DosageFormId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DosageFormId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DosageFormId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
