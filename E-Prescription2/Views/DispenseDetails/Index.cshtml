@model IEnumerable<E_Prescription2.Models.DispenseDetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Dispensing Details</h1>

<p>
    <a asp-action="Create"><button type="button" class="btn btn-outline-info">Create New</button></a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PharmacistUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PharmacyRecords)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PharmacistUser.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PharmacyRecords.PharmacyId)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.DispenseId"><i class="fa-sharp fa-solid fa-pen-to-square text-info"></i>Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.DispenseId"><i class="fa-sharp fa-solid fa-circle-info text-primary"></i>Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.DispenseId"><i class="fa-sharp fa-solid fa-trash text-danger"></i>Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
