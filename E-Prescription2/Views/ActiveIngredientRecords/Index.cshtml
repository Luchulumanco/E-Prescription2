@model IEnumerable<E_Prescription2.Models.ActiveIngredientRecord>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead class="table-primary">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActiveIngredientName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActiveIngredientName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActiveIngredientId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ActiveIngredientId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ActiveIngredientId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
